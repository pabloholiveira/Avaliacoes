# O painel é ferramenta interna: publicá-lo deixaria qualquer um que
# descobrisse o endereço abrir o gerador. Ele roda local, via
# `python3 -m http.server 8080`. Faça backup dele pelo Time Machine —
# o git não guarda mais este arquivo.
admin.html

# Backups automáticos do editor
*.bak.*

# QR codes gerados — são derivados dos JSONs, não precisam ir pro repositório
qrcodes/

# Backups da carteira exportados pelo painel (contêm todos os seus clientes)
carteira-*.json

# Ambiente local
.vscode/
__pycache__/
*.pyc
.DS_Store
